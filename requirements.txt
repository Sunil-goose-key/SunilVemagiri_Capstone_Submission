# Core Dependencies
# NOTE: the pack's exact pins (langchain==0.1.0 / langchain-community==0.0.10 /
# langchain-openai==0.0.7) are internally inconsistent -- langchain 0.1.0 requires
# langsmith<0.1.0, but every langchain-core release new enough to satisfy the other
# pins requires langsmith>=0.1.0. Re-pinned to the nearest self-consistent, verified-
# installable versions within the same 0.1.x line (confirmed working on Python 3.10.9,
# recorded here and in the PRD revision log as a pack correction, not a design change).
langchain==0.1.20
langgraph==0.0.20
langchain-community==0.0.38
langchain-openai==0.0.8

# Vector Database & Embeddings
# NOTE: bumped from the pack's pinned 0.3.21 -- that release imports `pydantic.BaseSettings`,
# a pydantic-v1-only API removed in pydantic v2 (moved to the separate pydantic-settings
# package). chromadb 0.3.21 predates the pydantic v2 migration entirely and cannot run
# alongside fastapi==0.104.0 + pydantic==2.5.3 (also both pack requirements). Bumped to the
# nearest chromadb release with pydantic v2 support. Recorded as a pack correction.
chromadb==0.4.24
# NOTE: bumped from the pack's pinned 2.2.2 -- that release imports `cached_download` from
# huggingface_hub, which was removed from huggingface_hub well before the version pulled in
# by the also-pinned (and unpinned-transitively-current) transformers/torch stack. Pinning
# huggingface_hub backward instead would conflict with those. Recorded as a pack correction.
sentence-transformers==3.0.1

# Data Processing
pandas==2.0.0
numpy==1.24.0
scikit-learn==1.3.0

# Web Framework
fastapi==0.104.0
uvicorn==0.24.0
# NOTE: bumped from the pack's pinned 2.0.0 -- fastapi==0.104.0 explicitly
# excludes pydantic 2.0.0/2.0.1/2.1.0. Recorded as a pack correction.
pydantic==2.5.3
python-multipart==0.0.6

# LLM & API
# NOTE: bumped from the pack's pinned 1.0.0 -- langchain-openai==0.0.7 requires
# openai>=1.10.0,<2.0.0, so 1.0.0 is not installable alongside it. Recorded in
# the PRD revision log / setup notes as a pack correction, not a design choice.
openai==1.12.0
requests==2.31.0
python-dotenv==1.0.0
aiohttp==3.9.0

# Monitoring & Logging
prometheus-client==0.18.0
python-json-logger==2.0.7

# Database
psycopg2-binary==2.9.9
SQLAlchemy==2.0.0

# Testing & Quality
pytest==7.4.0
pytest-asyncio==0.21.0
pytest-cov==4.1.0
flake8==6.1.0
black==23.10.0

# Development
jupyter==1.0.0
ipython==8.15.0

# Utilities
tqdm==4.66.0
PyYAML==6.0.1
